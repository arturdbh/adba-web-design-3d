/**
 * ADBA DESIGN - CLEAN OBSIDIAN ENGINE
 * 3D Background effect deactivated for maximum typography legibility and 100% pitch black background.
 */

function initHero3D() {
  // 3D background deactivated
  return;
}

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initHero3D);
} else {
  initHero3D();
}
